# Travel-Cost-Optimizer
This program finds the minimum cost to travel between stations using a recursive approach. It uses a cost matrix and tries all possible intermediate stations. The recursion computes the cheapest combination of paths and returns the minimum total cost
